export const MENU_TYPES = ['every', ...MENU_DROPDOWN_VALUES] as const;

// the string 'every' is a literal value included as the first element of the MENU_TYPES array. 

// For example, 'every' could represent a specific category within the application (like a calendar app), which means an option for recurring events that happen on every occurence of a given time unit.

// 'every' could also represent a default value